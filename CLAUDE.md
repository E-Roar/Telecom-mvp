@AGENTS.md

# Additional Rules for Claude/Opencode

- Read AGENTS.md completely before writing any code.
- Follow the file structure EXACTLY as specified.
- Do NOT create any files outside the specified structure.
- When in doubt, refer back to AGENTS.md.
- Build components in dependency order: types → mockdb → seed → hooks → UI components → pages → agent.
